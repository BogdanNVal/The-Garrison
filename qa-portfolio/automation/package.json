{
  "name": "the-garrison-qa-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright smoke tests for The Garrison",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.49.0"
  }
}
