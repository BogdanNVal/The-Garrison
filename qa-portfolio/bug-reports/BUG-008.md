# BUG-008 — Fresh clone missing page components

| | |
|--|--|
| Severity | Medium |
| Priority | Medium |
| Status | Open (worked around for testing) |
| Build | Clean clone |
| Area | Setup / packaging |

## Summary

Pages include files under `components/` (header, footer, home, menu, etc.), but those weren’t in the repo when I cloned it. README screenshots show a full UI that a clean clone doesn’t serve.

## Steps

1. Clone the repo.
2. Start with Docker.
3. Open login / home.
4. Check for missing include warnings / broken layout.

## Expected

Everything needed to run is in the repo, or the README says how to get it.

## Actual

Missing components / assets. App wasn’t usable until stubs were added.

## Notes

For this testing I used minimal Bootstrap stubs under `src/components/` so I could still exercise login, admin, and reservations. Full original template still isn’t here.
