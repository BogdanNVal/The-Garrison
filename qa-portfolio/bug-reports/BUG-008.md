# BUG-008 — Fresh clone was missing page components

| | |
|--|--|
| Severity | Medium |
| Priority | Medium |
| Status | Fixed on this branch (components restored) |
| Build | Old main vs current branch |
| Area | Setup / packaging |

## Summary

Pages include files under `components/` (header, footer, home, menu, etc.). Those weren’t in the repo on first clone, so home/login couldn’t render properly.

## Steps

1. Clone old main.
2. Start Docker.
3. Open login / home.
4. Missing include warnings / broken layout.

## Expected

Required component files are in the repo.

## Actual (then)

Missing `components/*.php`. Had to use temporary stubs to keep testing.

## Notes

Real component files from an older project copy were put back under `src/components/` (including `why-us.php` and `book_a_table.php`). `.gitignore` does not ignore that folder — only uploaded images in `src/assets/img/menu/*` and `.env`.
